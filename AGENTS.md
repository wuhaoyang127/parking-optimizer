# AGENTS.md

## 1. 项目使命与边界

本仓库研究智能停车场中的动态车位分配、纵深车位阻挡与移位调度，目标是形成可解释、可复现、可仿真验证，并能进一步接入停车场管理系统的算法原型。

立项目标保持不变：

- 提高需求满足率和有效车位时空利用率；
- 降低纵深停车产生的移位次数与移位距离；
- 改善等待、行驶和出库效率；
- 形成可验证、可展示、可扩展的停车调度系统。

本项目不实现真实车辆感知、转向控制、碰撞规避或自动驾驶轨迹跟踪。路网模块只处理管理层面的可达性、路径成本和通道资源占用。

原始申请书与旧算法文档是待审查材料，不是正确性来源。允许保留、修改或否定其中任何算法，但必须记录证据和理由，并证明调整后的路线仍服务于上述立项目标。

## 2. 用户协作与沟通

### 2.1 先理解，再行动

复杂任务开始前，先用简洁语言说明：

- 对任务目标的理解；
- 本次包含与不包含的范围；
- 准备读取或修改的文件；
- 计划采用的验证方法；
- 尚存的不确定项。

当歧义会改变研究范围、算法路线、数学定义、目标函数、约束、指标、数据含义、公共接口、重要依赖或交付形式时，必须先询问用户，不得自行猜测后实施。

对不影响研究含义和最终结果的局部细节，可选择最保守、最易验证的方案，并记录决定，不要用大量零碎问题反复打断用户。

### 2.2 不盲目顺从

发现用户提出的算法、假设或做法不合理时，必须指出证据、风险和替代方案。不得为了迎合用户而保留错误模型、伪造优势、淡化失败或把推测写成事实。

不得轻易声称“已经百分之百理解”。应列出当前理解和剩余不确定项；关键需求未确认前，不进行大规模或不可逆修改。

### 2.3 面向本科生解释

难度可以高，但必须让大二本科生能够逐步学会：

1. 先讲现实问题和直观思路；
2. 再讲数学定义、公式、变量与单位；
3. 再给伪代码、数据流和代码位置；
4. 关键设计配一个极小可手算或可枚举的例子。

## 3. 启动顺序、阶段门禁与项目状态

### 3.1 每次新对话的读取顺序

新任务或新对话开始前，依次读取：

1. `AGENTS.md`；
2. `PROJECT_STATUS.md`；
3. `.agent/PLANS.md`；
4. 若 `current_exec_plan` 不为 `null`，读取其指向的当前 ExecPlan；为 `null` 时跳过，不得猜测路径；
5. 若 `latest_handoff` 不为 `null`，读取其指向的最近阶段交接摘要；为 `null` 时跳过；
6. 与本任务直接相关的权威文档和代码。

只有在以下情况之一成立时才停止实施并先报告：

- 当前请求不是 `PROJECT_STATUS.md` 记录的当前阶段或唯一下一步；
- 当前任务所需的前置阶段尚未批准；
- `current_stage_blockers` 中存在直接阻止当前任务的问题。

`downstream_blockers` 只阻止进入其标明的后续阶段，不阻止当前阶段为解决这些问题而开展工作。

### 3.2 阶段门禁

项目按以下阶段推进：

- **A：研究与路线决策**——审查旧方案、核验文献、比较候选路线；
- **B：模型与工程设计**——冻结问题定义、模型、数据契约、架构和实验协议；
- **C：核心实现与验证**——实现阶段 B 批准的系统、基线、核心算法、测试和实验；
- **D：应用与交付**——实现稳定接口、真实数据适配、展示系统和交付材料。

不得跨阶段大规模实现。阶段未通过验收并经用户批准，不得进入下一阶段。

复杂任务、跨多个模块、改变数学含义、引入重要依赖或预计超过一小时的任务，必须按照 `.agent/PLANS.md` 建立 ExecPlan。

### 3.3 `PROJECT_STATUS.md`

`PROJECT_STATUS.md` 是项目当前状态的唯一入口，只保存当前有效状态，不替代 ExecPlan、研究文档、阶段交接摘要或 Git 历史。

必须维护：

- 当前阶段、里程碑和验收状态；
- 当前 ExecPlan、最近交接摘要和下一提示词的明确路径；
- 已完成并验证的内容；
- 已批准和已冻结的决定；
- 尚未冻结的问题；
- 数据、代码、测试和实验状态；
- `current_stage_blockers`、`downstream_blockers`、重要风险和当前唯一下一步；
- 最近不超过 10 条的重要变更。

允许的阶段状态统一使用：

- `not_started`
- `in_progress`
- `awaiting_review`
- `approved`
- `blocked`
- `superseded`

只有项目主线程或用户明确指定的协调线程可以直接提交 `PROJECT_STATUS.md` 更新；其他线程只能提出更新建议。阶段切换、核心路线、冻结定义和完成状态必须经用户确认。

为保证不依赖第三方 YAML 库也能执行严格检查，`PROJECT_STATUS.md` 的 front matter 保持扁平：
`current_stage_blockers` 使用单行 JSON 字符串数组，`downstream_blockers` 使用单行 JSON 对象数组。
每个下游阻断项必须同时包含非空 `target_stage` 和 `issue`，且 `target_stage` 只能是 `A`、`B`、`C` 或 `D`。
不得改成自检脚本无法完整解析的缩进嵌套结构。

不得把计划、推测、未运行测试或未验收成果写成已完成。测试失败，或 `current_stage_blockers` 尚未清除时，不得标记当前阶段完成。下游阻断项应在进入对应阶段前清除。

## 4. 单一事实来源、冻结与版本管理

### 4.1 单一事实来源

| 信息类型 | 权威来源 |
|---|---|
| 项目整体状态、全局风险和唯一下一步 | `PROJECT_STATUS.md` |
| 当前阶段详细计划、过程和局部决策 | 当前 ExecPlan |
| 问题定义、数学模型和变量 | `docs/model/` 中获批文档 |
| 数据字段、单位和校验 | `docs/data/` 与机器可读 Schema |
| 实验协议和评价口径 | `docs/experiments/` 中冻结协议 |
| 参数、场景和随机种子 | `configs/` |
| Python 依赖 | `pyproject.toml` 与锁定文件 |
| 指标实际计算 | `src/parking_opt/evaluation/`，并与模型文档一致 |
| 原始运行结果 | `outputs/` 中带元数据的运行产物 |

发现冲突时不得任选一个继续，应先报告并依据权威来源修正。

### 4.2 核心定义冻结

阶段 B 通过后，下列内容视为冻结基线：

- 研究问题与系统边界；
- 普通车位、纵深车位、阻挡、移位和缓冲位定义；
- 利用率、等待、行驶和出库延误等指标口径；
- 硬约束、目标优先级和信息可见范围；
- 输入输出字段、单位和 Schema；
- 离线、在线和动态求解模式的区别；
- 正式实验协议。

确需变更时，必须先提交变更说明，包含：

1. 原定义的问题和证据；
2. 新旧差异；
3. 受影响的文档、代码、测试和实验；
4. 是否需要重跑历史实验；
5. 回滚方案。

获得用户确认后才能实施。

### 4.3 过期文档

过期文档不得继续作为权威来源。应在文件顶部标记：

```yaml
status: superseded
superseded_by: <新文件路径>
superseded_date: YYYY-MM-DD
```

并移入 `docs/archive/`。`PROJECT_STATUS.md` 不得继续引用过期文件，新对话默认不读取 `docs/archive/`。

### 4.4 Git 与恢复点

建议分支：

- `main`：已通过验收的稳定内容；
- `stage-a-research`：阶段 A；
- `stage-b-model`：阶段 B；
- `feature/<功能名>`：阶段 C/D 的独立功能。

不直接在 `main` 上进行大规模修改。删除文件、重命名目录、更换核心数据结构、改变公共接口或大规模重构前，必须确认工作区干净并创建可回滚提交。

多个 Codex 线程不得同时编辑同一分支或同一批文件。

## 5. 研究与建模硬约束

### 5.1 文献与证据

- 优先使用论文原文、出版社页面、作者公开代码、官方文档和权威数据库；
- 核心结论记录题名、作者、年份、出处、DOI或稳定链接、访问日期及其实际支持的内容；
- 只有摘要或元数据时必须明确标注；
- 不得伪造文献、DOI、公式、真实数据或实验结果；
- “优于现有算法”必须限定比较对象、信息条件、场景、指标、计算预算和统计结果。

### 5.2 信息边界

仿真器可以保存完整事件真值，但时刻 `t` 的在线或动态算法只能访问：

- 当前停车场状态；
- 已发生的到达与离开；
- 已知预约；
- 用户实际提供的预计离场时间；
- 明确标记为预测值的短期需求或离场估计。

禁止把未来真实到达、真实停车时长、真实离场时间或未来最优结果泄漏给在线或动态算法。

必须区分：

1. 离线全信息基准或 oracle；
2. 在线即时策略；
3. 阶段 A/B 最终批准的动态优化方式。

### 5.3 纵深车位与移位

纵深车位使用 `stack_group_id` 和 `depth` 表示，`depth=1` 表示最靠近通道。普通独立车位是深度为 1、独立分组的特例。

阻挡不能只写成模糊风险分数，必须明确：

- 阻挡发生条件；
- 缓冲位是否存在及容量；
- 一次移位的起点、终点、距离、时间和次数；
- 被阻挡车辆离场时的实际事件流程；
- 未知离场时间的估计或鲁棒处理；
- 是否允许为了提高利用率而主动接受未来移位。

### 5.4 利用率和目标

至少分别报告：

- 占用率；
- 车位时空利用率；
- 需求满足率；
- 单位时间吞吐量。

不得把占用率、利用率、满足率和停车场结构容量混为一谈。

目标优先考虑分层或词典序：

1. 保证可行、安全和必要服务；
2. 最大化需求满足率或有效车位时空利用率；
3. 最小化移位次数和移位距离；
4. 再优化行驶、等待、拥堵、出库延误和决策稳定性。

如使用加权和，必须说明单位、归一化、权重来源和敏感性分析。

## 6. 候选路线与独立决策

当前优先考察的候选路线为：

> 路网成本预处理 + 可校准的事件仿真 + 小规模高质量基准 + 动态车位分配优化 + 确定性降级策略。

可能的具体工具包括 NetworkX、SimPy 和 OR-Tools CP-SAT，但它们在阶段 A/B 通过前都不是强制技术选型。

阶段 A 必须真正比较候选路线，不得先定结论再找证据。阶段 B 根据批准路线冻结模型和工程设计。阶段 C 的提示词只是通用模板，执行前必须依据阶段 B 的批准结果复核或重生成，不得因为模板中出现某个算法名就默认采用。

旧 Dijkstra-SA 可在定义完整、实现可复现且比较公平时作为对照，不自动成为主算法。ARIMA、随机森林、强化学习、深度学习或复杂路径规划不得因为“更先进”而被强行加入。

## 7. 仓库、数据、编码与依赖

### 7.1 仓库和数据

- `references/original/`：原始申请书和旧算法文档，只读；
- `references/papers/`：合法获取的论文、元数据或作者代码说明；
- `data/raw/`：原始实测数据，只读，不覆盖；
- `data/synthetic/`：仿真数据，文件名和元数据必须标记 `synthetic`；
- `outputs/`：运行产物，不作为源代码或手工编辑的结论来源；
- 不在代码中写死个人电脑绝对路径；
- 不把仿真数据描述为真实采集数据；
- 不提交 `.env`、密钥、令牌、真实车牌或未经脱敏的个人数据；
- 新增数据文件时遵守 `.gitignore`，必要时先询问用户是否允许纳入版本控制。

### 7.2 编码

- 默认 Python 3.11 64 位；
- 使用 `src/` 布局，核心包为 `parking_opt`；
- 领域模型、输入输出、路网、需求、仿真、策略、优化、评估和展示分层；
- 公共函数和类有类型注解与简洁文档字符串；
- 核心逻辑不得只存在于 Notebook；
- 优先小函数、显式状态和配置对象，避免隐藏全局状态与魔法数字；
- 错误信息指出具体字段、约束或文件；
- 不做与当前任务无关的重构、批量重命名、全仓库格式化或依赖升级。

### 7.3 依赖

添加依赖前必须：

1. 说明用途及为什么现有依赖不足；
2. 检查许可证、Windows 和 Python 3.11 兼容性；
3. 写入 `pyproject.toml` 并锁定可复现版本；
4. 更新安装说明；
5. 增加最小导入或功能测试。

不得一次安装尚未使用的大量库。引入第三方代码时记录来源、许可证、修改范围及其与自研代码、论文和软著的边界。

### 7.4 文档数量

不得为了显得工作量大而生成重复文档。新建文档前确认：

- 现有权威文档无法合理容纳；
- 有明确读者和长期用途；
- 与现有文档关系清楚；
- 不会产生新的重复事实来源。

## 8. 测试、降级与失败处理

每次改变行为时增加或更新测试。至少覆盖：

- 输入校验；
- 路网可达性；
- 车位占用不重叠；
- 纵深阻挡与移位；
- 信息泄漏；
- 极小人工最优例；
- 固定种子复现；
- 指标边界；
- 超时和降级。

当求解器超时、无可行解、输入不完整、路网不连通或预测模块不可用时，可以使用批准的降级策略，但必须记录：

- 原算法和求解状态；
- 降级原因、时间和涉及对象；
- 实际使用的策略；
- 对结果解释的影响。

不得静默降级后仍把结果标成主算法结果。

失败案例必须保留，包括无解、超时、缓冲位不足、主算法不如基线、预测严重错误和高负载崩溃等。不得只展示成功结果。

## 9. 实验冻结、公平性与可追溯

正式实验前先冻结：

- 比较算法与版本；
- 数据划分和场景；
- 调参规则；
- 随机种子；
- 指标与统计方法；
- 运行时间预算；
- 失败与超时处理；
- 图表生成规则。

开发调试、参数选择和最终测试使用不同场景或种子。看到结果后不得为了美化主算法而改变指标、删除不利场景或给予基线不公平参数。

不同算法比较时必须使用相同布局、车辆事件、约束、可见信息和指标计算方式。离线 oracle 必须单独标注，不能与在线算法假装处于相同信息条件。

每次正式运行至少保存：

- `run_id`、`scenario_id`、`schema_version` 和 `algorithm_version`；
- Git 提交；
- 配置和随机种子；
- 输入数据哈希；
- Python、依赖、求解器、操作系统和硬件摘要；
- 开始结束时间、求解状态和降级记录；
- 完整原始指标。

结论必须区分“证明最优”“限时可行”“当前仿真场景平均更优”和“真实环境尚未验证”。

## 10. 学习说明、交接与停止条件

每个核心模块完成时，应提供一份简洁教学说明，包含：

- 现实问题和核心思想；
- 输入、输出和关键假设；
- 公式、变量、单位；
- 伪代码或流程；
- 极小例子；
- 代码入口和运行方法；
- 常见错误和验证方法。

关键算法必须能由组员通过中文伪代码、变量表、流程图、复杂度说明和极小例子讲清楚。

每阶段结束在 `docs/handoffs/` 生成唯一交接摘要，使用统一模板，记录已批准决定、关键文件、实际测试、失败案例、限制、下一阶段条件和待用户决定事项。`PROJECT_STATUS.md` 只引用摘要，不复制详细过程。

出现以下情况应停止扩展并报告：

- 当前阶段验收目标已经满足；
- 新功能不影响核心研究问题；
- 新算法没有明确基线和验证方式；
- 所需数据目前无法获得；
- 复杂度明显超过项目收益；
- 只是为了“看起来更高级”；
- 会危及结题时间或核心系统稳定性。

## 11. Codex 执行方式

开始任务时：

1. 按第 3.1 节读取文件；
2. 用不超过十行复述当前状态、本次范围、文件和验证方式；
3. 有阻断性歧义时先询问；
4. 无阻断问题时按最小可验证步骤执行。

完成任务时：

- 列出修改文件；
- 列出实际运行命令及结果；
- 说明已验证内容、未完成项和风险；
- 提出 `PROJECT_STATUS.md` 更新建议；
- 自查 Diff，不覆盖原始材料；
- 不自动进入下一阶段。

独立文献检索或代码审查可以委派，但主线程必须核验来源和整合结果。多个代理不得同时编辑同一文件。

## 12. 完成定义

一项任务只有在以下条件满足时才算完成：

- 行为符合当前阶段和冻结规范；
- 文档、代码、配置和 Schema 一致；
- 测试已实际运行并通过，或失败被明确记录；
- 没有未来信息泄漏、伪造数据、静默降级或未说明硬编码；
- 结果可通过明确命令复现；
- 决策、风险、失败案例和状态更新已记录；
- `PROJECT_STATUS.md` 与实际代码、测试和权威文档一致；
- 教学说明和交接材料足以让组员继续工作；
- 用户已完成必要验收。
