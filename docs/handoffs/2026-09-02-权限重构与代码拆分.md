# 交接摘要：代码全量拆分 + 权限体系重做（2026-09-02）

## 一、本次对话完成的工作

### 1. 全库 Python 文件按功能拆分（≤200 行强制规则）
- 所有源码/测试/脚本单文件均 ≤200 行，`scripts/check_file_lines.py` 自检全绿。
- 规则已写入 `AGENTS.md` 3.2.1：**以后所有改动都必须符合，超 200 行必须按功能拆，一个功能一个文件，禁止胡乱拆**。
- 主要拆分：`src/ui/common/` 包、`src/ui/pages/` 包、`src/auth/` 包、`src/viz/` 包、`src/local_compute/` 包、
  `simulation/engine.py` 三个 mixin、`strategies/mosa.py` 五个文件、`io/realtime_io.py` 三个文件、
  `scripts/validate_starter_package.py`（starter_checks 包）、`scripts/exp_risk_scoring.py`、
  `tests/` 大测试文件按主题拆分。

### 2. 权限体系全面重做（板块级 + 功能级）
- **9 个板块**（页面导航可见性）：settings/system/layout/path/metrics/history/algo_import/status/feedback。
- **14 项功能权限**（逐按钮开关）：编辑参数/导入需求/导出需求/云端运行/本地计算/删除本地任务/导出结果/
  删除历史/用户管理/数据管理/上传算法/查看状态/提交反馈/反馈管理。
- 内置三角色按推荐矩阵：**管理员**全开；**操作员** 8 板块（无系统设置）+ 运营功能（含本地计算与删除历史）；
  **访客** 6 板块 + 仅云端运行 + 提交反馈。
- **自定义角色**：管理员在「系统设置 → 用户管理 → 🧩 自定义角色权限」同时勾选板块和功能权限，
  保存后同步所有 custom 用户；permissions 存 `{sections, features}`。
- 定义在 `src/ui/common/permissions.py`；`resolve_role(role_name, permissions)` 解析（兼容旧数组格式）。

### 3. 数据库迁移（已直连执行）
- **迁移 13 已执行成功**：`users.permissions JSONB` + `app_settings` 模板表 +
  `get_custom_sections` / `save_custom_sections` RPC + login/validate/list_users 返回 permissions +
  update_user_role 支持 custom。
- **迁移 14 已执行成功**：旧数组数据升级（幂等）。
- **login_user 已修复为双格式密码兼容**（bcrypt `$2` 开头用 crypt 校验；sha256 老用户自动升级 bcrypt），
  与 login_worker 行为一致。`migrations/01_setup.sql` 和 `13_*.sql` 已同步该逻辑，防止重跑迁移覆盖坏。

### 4. 功能与修复
- 删除用户按钮增加二次确认。
- 修复 `secrets.toml` 裸连接串导致的 TOML 解析失败（已改为 `DATABASE_URL = "..."`）。
- 修复拆分引入的相对路径错误：布局说明文档、算法上传目录（parents[2]→parents[3]）。
- 修复 `import *` 不携带下划线内部名的 NameError：`ui/common/__init__.py` 和 `viz/colors.py` 显式 `__all__`。

### 5. 冗余清理（经用户批准）
- 删：`starter_checks/_main.py` 未用 import Any；`ranking.py` 未用 import Iterable 与无引用函数 `metrics_field_names`。
- 保留：`road_io.load_demand/save_road_network`、`timeline.build_timeline`（用户原则：类似功能/数据安全不删）。

## 二、当前状态
- 分支：`stage-d-deliver`，最新提交 c76b580。
- 测试：**154 passed**；行数检查全绿；启动包自检通过。
- 本地 `.streamlit/secrets.toml` 现含 SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_KEY / DATABASE_URL（gitignore 已忽略）。
- 数据库：迁移 13/14 已执行，login_user 双格式已生效。

## 三、待办（下个对话接手）
1. **云端 Streamlit 重新部署**（多次 bug 修复需重新 Deploy 才生效，Reboot 不够）。
2. 用户验收权限体系：管理员勾选自定义角色板块/功能 → 保存 → 换 custom 账号验证导航和按钮；
   验证访客/操作员默认矩阵。
3. 企业交付前人工待办（老事项）：删旧 publishable key、改管理员强密码、数据授权协议、试点基线、
   `docs/企业可用性检查清单.md`。
4. 若再出现 NameError/路径类报错：优先检查拆分后文件的 `import *` 下划线名导出、`parents[n]` 层级。

## 四、验收建议路径
1. 云端重新部署 → 管理员登录 → 系统设置勾选自定义权限保存（验证不再报 PGRST202）。
2. 手机 114514/114514 登录（custom 角色）→ 验证板块可见性和按钮开关。
3. 访客账号 → 验证只能云端运行 + 提交反馈，无本地计算选项。
4. 操作员 222 → 验证可本地计算/删除历史/看不到系统设置。
