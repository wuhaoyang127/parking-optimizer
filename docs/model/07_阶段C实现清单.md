---
status: draft
stage: B
created: 2026-08-04
target: stage-c-implementation
---

# 07 阶段C实现清单

---

## 1. 阶段C子任务映射

| 原C模板 | 阶段B冻结后 | 状态 |
|---|---|---|
| C0_阶段C总控 | ✅ 直接使用 | 总控不变 |
| C1_环境与领域模型 | ✅ 直接使用 | 对应 docs/model/01-03 |
| C2_路网与仿真数据 | ✅ 直接使用 | 对应 docs/data/01 |
| C3_仿真与基线 | ✅ 直接使用 | SimPy + 6基线 |
| C4_精确或高质量基准 | ✅ 改用 "CP-SAT 离线基准" | 不再叫"精确或高质量" |
| C5_动态核心算法 | ✅ 改用 "在线贪心策略" | 不再叫"动态核心" |
| C6_实验统计与报告 | ✅ 直接使用 | 对应 docs/experiments/ |
| C7_全面复核 | ✅ 直接使用 | — |

---

## 2. 阶段C实现顺序

```
C1: domain/ (Spot, Vehicle, RoadNetwork, Event) + tests
C2: routing/ (PathEngine) + io/ (JSON读写)
C3: simulation/ (SimPy engine, ParkingLot, relocation) + tests
    + strategies/ (FCFS, nearest, random 三个简单基线)
C4: optimization/ (CP-SAT 离线基准, 仅小规模)
C5: strategies/greedy.py (主方法)
    + strategies/departure_order.py (离场顺序贪心基线)
    + strategies/sa_optimizer.py (Dijkstra-SA改造)
C6: evaluation/ (metrics) + cli/ (命令行入口)
    + 运行全部实验矩阵
C7: docs/ 更新, 阶段D输入准备
```

---

## 3. pyproject.toml（阶段C1初始化）

已在 `docs/architecture/01_软件架构.md` 第4节定义。

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -e .
```

---

## 4. 阶段C的唯一下一步

执行 `prompts/C/C0_阶段C总控.md`，按 C1→C7 顺序逐步实现。
