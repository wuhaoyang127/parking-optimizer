# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略）
# 这些变量会被 docker-compose.yml 注入到 Streamlit 容器，
# 与 .streamlit/secrets.toml 二选一即可（环境变量优先）。
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=sb_publishable_你的匿名公钥
