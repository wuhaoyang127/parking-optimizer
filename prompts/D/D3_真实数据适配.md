# D3：真实数据导入与校准接口

开始前先读取 `AGENTS.md`、`PROJECT_STATUS.md` 和当前阶段 ExecPlan。仅当前置阶段未通过、当前任务不是唯一下一步，或 `current_stage_blockers` 直接阻止本任务时才停止；下游阻断项不阻止当前工作。

只完成 D3。

要求：

- 提供停车场平面与路网数据模板；
- 提供车位、入口出口、纵深关系和缓冲位模板；
- 提供车辆到达/离场、占用状态和移位日志模板；
- 建立字段映射、缺失值、异常值和单位校验；
- 原始数据只读；
- 生成数据质量报告；
- 支持只有布局、没有车辆历史的最小模式；
- 写明哪些参数无法从当前数据识别；
- 不伪造真实校准结果；
- 停止。
