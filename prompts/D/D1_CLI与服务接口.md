# D1：CLI 与稳定服务接口

开始前读取 `AGENTS.md`、`PROJECT_STATUS.md` 和当前阶段 ExecPlan。仅当当前任务的前置条件未满足或 `current_stage_blockers` 直接阻止本任务时才停止；下游阻断项不阻止当前工作。

只完成 D1。

要求：

- 提供布局校验、单场景仿真、批量实验和报告生成命令；
- 提供与具体界面框架解耦的稳定 Python 服务接口；
- 输出目录可配置；
- 错误信息清楚；
- 命令有 `--help`；
- 增加 CLI 集成测试；
- 不做网页或桌面界面；
- 更新用户文档并停止。
